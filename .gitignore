node_modules/
dist/
*.tsbuildinfo
.DS_Store

# One run's artifacts — trace, summary, report. Regenerated by `npm run audit`.
# .invariant/baseline.json IS committed: it is the ledger of what the gate accepts.
.invariant/last/
